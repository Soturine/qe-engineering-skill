# Skill Scripts

Runtime helper scripts will live here only when they expose stable deterministic engine capabilities (inventory, validation, rendering, etc.).

Scripts must not duplicate trust logic in ad-hoc prompt helpers. They should call the versioned core package and return structured, auditable results.
