# Skill References

This directory will contain packaged reference material selected from the repository's normative policies for progressive disclosure at runtime.

Do not manually fork policy text here during development. Prefer a future packaging/build step that versions/copies approved references from `docs/` so the Agent Skill cannot drift from repository governance.
